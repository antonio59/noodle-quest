# Cloudflare Pages redirects — https://developers.cloudflare.com/pages/configuration/redirects/
# Keep the old Netlify Function URL working: rewrite (200, preserves POST
# method and body) to the Pages Function.
/.netlify/functions/send-report-email  /api/send-report-email  200
# SPA fallback
/*  /index.html  200
